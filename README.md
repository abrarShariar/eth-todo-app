# eth-todo-app


```
> truffle migrate 
// for deploying again
> truffle migrate --reset

> truffle console
> toDoList = await ToDoList.deployed()
> toDoList.taskCount()
> toDoList.createTask("My New Task")
> toDoList.tasks(1)
```